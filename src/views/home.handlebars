<div>
  <h1>{{title}}</h1>

  <div>
    {{#each products}}
      <div class="prodContainer">
      <h2>{{this.title}}</h2>
      <p>Código: {{this.code}}</p>
      <p>Precio: ${{this.price}}</p>
      <img
        src='{{this.thumbnail}}'
        alt='{{this.title}}'
        height='auto'
        width='100'
      />
      <p>Stock: {{this.stock}}</p>
      <p
        style='color: red; font-size: 20px; background: black; font: bold; padding: 4px;'
      >ID: {{this.id}}</p>
    {{/each}}
    </div>
</div>
</div>